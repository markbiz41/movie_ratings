== README

n this branch, add a MovieFavorite model to the app to keep track of a user's favorite movies. In the existing movie controller app, also add a 'favorite' and 'unfavorite' action to create or destroy an appropriate record showing the movie has been favorited by the logged in user.